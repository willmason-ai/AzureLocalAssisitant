apiVersion: v1
kind: Service
metadata:
  name: azure-local-dashboard
  namespace: azure-local-ops
  labels:
    app: azure-local-dashboard
spec:
  type: LoadBalancer
  selector:
    app: azure-local-dashboard
  ports:
    - port: 80
      targetPort: 5230
      protocol: TCP
      name: http
